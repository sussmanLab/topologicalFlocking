#ifndef globalHeader_H
#define globalHeader_H
//I am so, so sorry for this ugly cludge...it just stops accidental allocation of memory on available GPUs when CPU-only mode is requested

extern bool globalCudaDisable;


#endif
